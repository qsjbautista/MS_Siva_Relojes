spring:
  datasource:
#    initialize: true
    url: jdbc:h2:mem:auditoria;DB_CLOSE_DELAY=-1
    name:
    username:
    password:
#    data: classpath:/data-h2.sql
#    schema: classpath:/schema-h2.sql
  jpa:
    database: H2
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: none